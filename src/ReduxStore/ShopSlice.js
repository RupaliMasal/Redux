import { createSlice } from "@reduxjs/toolkit";

const ShopSlice=createSlice({
    name:"myShop",
    initialState:[],
    reducers:{
        addMyProduct:(state)=>{
           state.
        }
    }
})